export default function reducer(state, action) {
    switch (action.type) {


        default: {
            return state
        }
    }

    function update(state, mutations) {
        return {
            state, ...mutations
        }
    }
}